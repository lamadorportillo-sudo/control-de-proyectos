name: Publicar navegación completa del expediente

on:
  push:
    branches: [main]
    paths:
      - 'project-tabs-complete-v1.js'
      - '.github/workflows/deploy-project-tabs-complete-v1.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: control-contractual-project-tabs-complete
  cancel-in-progress: true

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main

      - name: Validar módulo
        run: node --check project-tabs-complete-v1.js

      - name: Integrar módulo en index y build
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          module='project-tabs-complete-v1.js'
          version='20260820-tabscomplete1'

          index=Path('index.html')
          html=index.read_text(encoding='utf-8')
          html=re.sub(r'<script\s+src=["\']'+re.escape(module)+r'(?:\?[^"\']*)?["\']\s*></script>\s*','',html,flags=re.I)
          pos=html.lower().rfind('</body>')
          if pos<0: raise SystemExit('No se encontró </body>')
          html=html[:pos]+f'<script src="{module}?v={version}"></script>\n'+html[pos:]
          index.write_text(html,encoding='utf-8')

          build=Path('build-pages.cjs')
          src=build.read_text(encoding='utf-8')
          src=re.sub(r"\s*\['project-tabs-complete-v1\.js','[^']+'\],?",'',src)
          anchor="  ['storage-quota-fix-v1.js','20260820-storagequota2'],"
          if anchor not in src:
              raise SystemExit('No se encontró punto de integración para las pestañas')
          src=src.replace(anchor,anchor+"\n  ['project-tabs-complete-v1.js','20260820-tabscomplete1'],")
          build.write_text(src,encoding='utf-8')
          PY

      - name: Validar build
        run: node --check build-pages.cjs

      - name: Guardar integración
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- index.html build-pages.cjs; then
            echo "Sin cambios adicionales."
          else
            git add index.html build-pages.cjs
            git commit -m "Integrar navegación completa del expediente [skip ci]"
            git push origin HEAD:main
          fi
