/* CONTROL CONTRACTUAL · CENTRO GLOBAL DE DEFICIENCIAS Y ALERTAS Y SEGUIMIENTO V1 */
(()=>{
'use strict';
if(window.__CC_ALERTS_CENTER_V2__) return;
window.__CC_ALERTS_CENTER_V2__=true;

const ST={active:false,search:'',level:'all',kind:'all',project:'all'};
const arr=v=>Array.isArray(v)?v:[];
const num=v=>Number(v)||0;
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const getDB=()=>{try{return db||null}catch{return null}};
const todayISO=()=>new Date().toISOString().slice(0,10);
const dateHN=v=>{if(!v)return'—';const a=String(v).slice(0,10).split('-');return a.length===3?`${a[2]}/${a[1]}/${a[0]}`:String(v)};
const money=v=>`L ${num(v).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const canEdit=()=>{try{return typeof roleCanEdit==='function'&&roleCanEdit()}catch{return false}};
function daysTo(date){if(!date)return 99999;return Math.ceil((new Date(date+'T23:59:59')-new Date())/86400000)}
function guaranteeAlert(date){const d=daysTo(date);if(d<0)return{level:'expired',label:'VENCIDA',days:d};if(d<=7)return{level:'urgent',label:'URGENTE',days:d};if(d<=15)return{level:'critical',label:'CRÍTICA',days:d};if(d<=30)return{level:'attention',label:'ATENCIÓN',days:d};if(d<=60)return{level:'warning',label:'PRÓXIMA A VENCER',days:d};return{level:'good',label:'VIGENTE',days:d}}
function projectProgress(project,contract,estimates){const base=num(contract?.currentAmount??project?.budget),gross=arr(estimates).reduce((s,e)=>s+num(e.gross),0);return base?Math.max(0,Math.min(100,gross/base*100)):num(project?.physicalProgress)}
function levelRank(level){return({expired:0,urgent:1,critical:2,attention:3,warning:4,info:5}[level]??9)}
function levelLabel(level){return({expired:'Vencido',urgent:'Urgente',critical:'Crítico',attention:'Atención',warning:'Seguimiento',info:'Informativo'}[level]||'Revisar')}
function actionLabel(tab){return tab==='guarantees'?'Revisar garantía':tab==='estimates'?'Revisar estimación o pago':tab==='visits'?'Revisar deficiencia en visita':tab==='procurement'?'Revisar adjudicación':'Abrir proyecto'}
function buildAlerts(){
 const d=getDB()||{},projects=arr(d.projects).filter(p=>!p.deletedAt),contracts=arr(d.contracts),estimates=arr(d.estimates),guarantees=arr(d.guarantees),visits=arr(d.visits),items=[];
 for(const p of projects){
   const c=contracts.find(x=>x.projectId===p.id),est=c?estimates.filter(e=>e.contractId===c.id):[],progress=projectProgress(p,c,est),finished=/finaliz|cerrad/i.test(String(p.status||'')),end=c?.end||p.end||'',left=end?Math.ceil((new Date(end+'T12:00:00')-new Date(todayISO()+'T12:00:00'))/86400000):null;
   if(!finished&&end&&progress<100){
     if(left<0)items.push({projectId:p.id,entityId:c?.id||p.id,project:p,tab:'contract',kind:'Plazo',level:'expired',title:'Proyecto con plazo vencido',detail:`Venció ${dateHN(end)} · avance ${progress.toFixed(1)}%`,fix:c?'Verificar la fecha final del contrato o registrar una ampliación aprobada.':'Completar o corregir la fecha final del proyecto.',date:end});
     else if(left<=30){const level=left<=7?'urgent':left<=15?'attention':'warning';items.push({projectId:p.id,entityId:c?.id||p.id,project:p,tab:'contract',kind:'Plazo',level,title:`Finalización en ${left} día${left===1?'':'s'}`,detail:`Fecha contractual ${dateHN(end)} · avance ${progress.toFixed(1)}%`,fix:'Revisar el plazo y registrar únicamente una ampliación debidamente aprobada.',date:end})}
   }
   for(const g of guarantees.filter(x=>x.projectId===p.id&&!x.voidedAt&&!x.voided_at)){
     const a=guaranteeAlert(g.end);if(a.level==='good')continue;
     items.push({projectId:p.id,entityId:g.id,project:p,tab:'guarantees',kind:'Garantía',level:a.level,title:`${g.type||'Garantía'} · ${a.label}`,detail:`${g.number||'Sin número'} · vence ${dateHN(g.end)}`,fix:'Actualizar la vigencia o registrar el endoso de ampliación de la garantía.',date:g.end});
   }
   if(c){
     const pending=est.filter(e=>!/(pagada|pagado|anulad)/i.test(String(e.status||''))).length;
     const pendingEst=est.filter(e=>!/(pagada|pagado|anulad)/i.test(String(e.status||''))).sort((a,b)=>String(b.end||b.start||'').localeCompare(String(a.end||a.start||'')));
     if(pending)items.push({projectId:p.id,entityId:pendingEst[0]?.id,project:p,tab:'estimates',kind:'Estimación',level:'warning',title:`${pending} estimación${pending===1?'':'es'} pendiente${pending===1?'':'s'}`,detail:'Falta completar aprobación, documentación o pago',fix:'Abrir la estimación pendiente y actualizar su estado o documentos.',date:pendingEst[0]?.end||pendingEst[0]?.start||''});
     const paidAdvance=num(c.advancePaid),amort=est.reduce((s,e)=>s+num(e.advanceApplied),0),advancePending=Math.max(0,paidAdvance-amort);
     if(advancePending>0)items.push({projectId:p.id,project:p,tab:'estimates',kind:'Anticipo',level:'info',title:'Anticipo pendiente de amortizar',detail:`Saldo por amortizar ${money(advancePending)}`,fix:'Revisar la amortización aplicada en las estimaciones del contrato.',date:c.advancePaymentDate||''});
   }
   const projectVisits=visits.filter(v=>v.projectId===p.id),visitsWithPending=projectVisits.filter(v=>arr(v.observations).some(o=>!/atendida|cerrada|resuelta/i.test(String(o?.status||'')))).sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))),pendingObs=projectVisits.reduce((n,v)=>n+arr(v.observations).filter(o=>!/atendida|cerrada|resuelta/i.test(String(o?.status||''))).length,0);
   if(pendingObs)items.push({projectId:p.id,entityId:visitsWithPending[0]?.id,project:p,tab:'visits',kind:'Deficiencia de obra',level:'attention',title:`${pendingObs} deficiencia${pendingObs===1?'':'s'} u observación${pendingObs===1?'':'es'} pendiente${pendingObs===1?'':'s'}`,detail:'Registrada en visita de obra · requiere corrección y seguimiento',fix:'Abrir las observaciones de la visita y marcar la corrección atendida.',date:visitsWithPending[0]?.date||''});
 }
 return items.sort((a,b)=>levelRank(a.level)-levelRank(b.level)||String(a.date||'').localeCompare(String(b.date||'')));
}
function setActive(active){ST.active=!!active;document.body.classList.toggle('cc-alerts-center-active',ST.active);window.dispatchEvent(new CustomEvent('cc:route-changed',{detail:{route:ST.active?'alerts':''}}))}
function closeOthers(){window.__ccContractsCenter?.close?.();window.__ccPaymentsCenter?.close?.();window.__ccGuaranteesCenter?.close?.();window.__ccVisitsCenter?.close?.();window.__ccReportsCenter?.close?.();window.__ccAuditCenter?.close?.()}
function ensureCss(){if(document.getElementById('cc-alerts-center-v1-style'))return;const s=document.createElement('style');s.id='cc-alerts-center-v1-style';s.textContent=`
.cca-page{display:grid;gap:12px}.cca-head h2{margin:3px 0;color:#f8fafc;font-size:24px}.cca-head p{margin:0;color:#8fa4bb;font-size:10px}.cca-eye{color:#70a9f7;font-size:8px;font-weight:900;letter-spacing:.13em}.cca-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.cca-kpi{padding:13px 14px;border:1px solid rgba(148,163,184,.12);border-radius:13px;background:#0b2137}.cca-kpi small{display:block;color:#8fa4bb;font-size:8px}.cca-kpi strong{display:block;color:#f8fafc;font-size:18px;margin-top:5px}.cca-tools{display:grid;grid-template-columns:minmax(230px,1fr) 170px 175px 230px auto;gap:8px;padding:10px;border:1px solid rgba(148,163,184,.12);border-radius:13px;background:#0a1d30}.cca-tools input,.cca-tools select{height:38px;border:1px solid #29415d;border-radius:9px;background:#071827;color:#f8fafc;padding:0 10px;font-size:9px;min-width:0}.cca-count{display:grid;place-items:center;color:#8fa4bb;font-size:8px}.cca-list{display:grid;gap:7px}.cca-item{display:grid;grid-template-columns:90px minmax(180px,.9fr) minmax(260px,1.6fr) 130px auto;gap:10px;align-items:center;padding:11px 12px;border:1px solid rgba(148,163,184,.11);border-radius:12px;background:#081827}.cca-item:hover{background:#0b1d30}.cca-project b{display:block;color:#76a9fa;font-size:8px}.cca-project strong{display:block;color:#f8fafc;font-size:9px;margin-top:2px}.cca-detail b{display:block;color:#e5edf7;font-size:9px}.cca-detail small{display:block;color:#71879e;font-size:7px;margin-top:3px}.cca-detail em{display:block;margin-top:6px;color:#bfdbfe;font-size:8px;font-style:normal;font-weight:750;line-height:1.35}.cca-priority{display:inline-flex;justify-content:center;padding:5px 7px;border-radius:999px;font-size:7px;font-weight:900;border:1px solid rgba(148,163,184,.15)}.cca-priority.expired,.cca-priority.urgent,.cca-priority.critical{color:#fecdd3;border-color:rgba(244,63,94,.25);background:rgba(244,63,94,.08)}.cca-priority.attention,.cca-priority.warning{color:#fde68a;border-color:rgba(245,158,11,.22);background:rgba(245,158,11,.07)}.cca-priority.info{color:#bfdbfe;border-color:rgba(96,165,250,.2);background:rgba(59,130,246,.06)}.cca-empty{padding:38px;text-align:center;color:#8fa4bb;border:1px solid rgba(148,163,184,.1);border-radius:13px;background:#081827}@media(max-width:1100px){.cca-kpis{grid-template-columns:repeat(3,1fr)}.cca-tools{grid-template-columns:1fr 1fr}.cca-tools input{grid-column:1/-1}.cca-count{display:none}.cca-item{grid-template-columns:80px 1fr 1.4fr auto}.cca-item>div:nth-child(4){display:none}}@media(max-width:650px){.cca-kpis{grid-template-columns:1fr 1fr}.cca-tools{grid-template-columns:1fr}.cca-tools input{grid-column:auto}.cca-item{grid-template-columns:1fr auto}.cca-item>div:nth-child(3){grid-column:1/-1}.cca-item>div:nth-child(4){display:block}.cca-item .btn{grid-row:1/3;grid-column:2}}
`;document.head.appendChild(s)}
function filtered(rows){const q=ST.search.trim().toLowerCase();return rows.filter(x=>ST.level==='all'||x.level===ST.level).filter(x=>ST.kind==='all'||x.kind===ST.kind).filter(x=>ST.project==='all'||x.projectId===ST.project).filter(x=>!q||[x.project.code,x.project.name,x.kind,x.title,x.detail].join(' ').toLowerCase().includes(q))}
function correctionContext(item,selector){setTimeout(()=>{const modal=[...document.querySelectorAll('.modal-bg')].at(-1),form=modal?.querySelector('form');if(form&&!modal.querySelector('[data-cca-correction]'))form.insertAdjacentHTML('beforebegin',`<div class="alert warning" data-cca-correction><b>Problema detectado:</b> ${esc(item.title)} · ${esc(item.detail)}<br><b>Para corregir:</b> ${esc(item.fix||actionLabel(item.tab))}</div>`);const field=modal?.querySelector(selector);field?.focus();field?.scrollIntoView({behavior:'smooth',block:'center'})},30)}
function openItem(item){
 setActive(false);try{
  const p=arr(getDB()?.projects).find(x=>x.id===item.projectId),c=arr(getDB()?.contracts).find(x=>x.projectId===item.projectId);if(!p)return;
  view.projectId=p.id;view.screen='project';view.tab=item.tab||'summary';renderApp();
  if(!canEdit()){setTimeout(()=>{try{toast('Tu acceso es de consulta. Puedes ver el problema; para modificarlo necesitas permiso de edición.')}catch{}},40);return}
  if(item.kind==='Plazo'){if(c&&typeof contractModal==='function'){contractModal(p,c);correctionContext(item,'#cEnd')}else if(typeof projectModal==='function'){projectModal(p);correctionContext(item,'#pEnd')}return}
  if(item.kind==='Garantía'){const g=arr(getDB()?.guarantees).find(x=>x.id===item.entityId);if(g&&typeof guaranteeModal==='function'){guaranteeModal(p,c,g);correctionContext(item,'#gEnd')}return}
  if(item.kind==='Estimación'){const e=arr(getDB()?.estimates).find(x=>x.id===item.entityId);if(c&&e&&typeof estimateModal==='function'){estimateModal(p,c,e);correctionContext(item,'#eStatus')}return}
  if(item.kind==='Deficiencia de obra'){const v=arr(getDB()?.visits).find(x=>x.id===item.entityId);if(v&&typeof visitObservationsModal==='function')visitObservationsModal(p,c,v)}
 }catch(error){console.warn(error)}}
function render(){if(!ST.active)return;ensureCss();const content=document.getElementById('content');if(!content)return;const all=buildAlerts(),list=filtered(all),projects=[...new Map(all.map(x=>[x.projectId,x.project])).values()].sort((a,b)=>String(a.name||'').localeCompare(String(b.name||''))),kinds=[...new Set(all.map(x=>x.kind))].sort(),critical=all.filter(x=>['expired','urgent','critical'].includes(x.level)).length,deadlines=all.filter(x=>x.kind==='Plazo').length,guarantees=all.filter(x=>x.kind==='Garantía').length,obs=all.filter(x=>/observ|deficiencia/i.test(x.kind)).length;content.innerHTML=`<section class="cca-page"><div class="cca-head"><span class="cca-eye">DEFICIENCIAS Y SEGUIMIENTO DE OBRA</span><h2>Deficiencias y seguimiento</h2><p>Cada asunto indica el problema, la corrección necesaria y abre directamente el dato que debes revisar.</p></div><div class="cca-kpis"><article class="cca-kpi"><small>Asuntos activos</small><strong>${all.length}</strong></article><article class="cca-kpi"><small>Críticos / urgentes</small><strong>${critical}</strong></article><article class="cca-kpi"><small>Plazos</small><strong>${deadlines}</strong></article><article class="cca-kpi"><small>Garantías</small><strong>${guarantees}</strong></article><article class="cca-kpi"><small>Observaciones</small><strong>${obs}</strong></article></div><div class="cca-tools"><input id="ccaSearch" placeholder="Buscar proyecto o asunto…" value="${esc(ST.search)}"><select id="ccaLevel"><option value="all">Todas las prioridades</option>${['expired','urgent','critical','attention','warning','info'].map(l=>`<option value="${l}" ${ST.level===l?'selected':''}>${levelLabel(l)}</option>`).join('')}</select><select id="ccaKind"><option value="all">Todos los controles</option>${kinds.map(k=>`<option value="${esc(k)}" ${ST.kind===k?'selected':''}>${esc(k)}</option>`).join('')}</select><select id="ccaProject"><option value="all">Todos los proyectos</option>${projects.map(p=>`<option value="${esc(p.id)}" ${ST.project===p.id?'selected':''}>${esc(p.code||'')} · ${esc(p.name||'Proyecto')}</option>`).join('')}</select><span class="cca-count">${list.length} de ${all.length}</span></div><div class="cca-list">${list.length?list.map((x,i)=>`<div class="cca-item"><div><span class="cca-priority ${esc(x.level)}">${levelLabel(x.level)}</span><small style="display:block;color:#71879e;margin-top:4px">${esc(x.kind)}</small></div><div class="cca-project"><b>${esc(x.project.code||'')}</b><strong>${esc(x.project.name||'Proyecto')}</strong></div><div class="cca-detail"><b>${esc(x.title)}</b><small>${esc(x.detail)}</small><em>Para corregir: ${esc(x.fix||actionLabel(x.tab))}</em></div><div><b>${dateHN(x.date)}</b><small style="display:block;color:#71879e">${actionLabel(x.tab)}</small></div><button class="btn primary" data-cca-open="${i}">${canEdit()?'Corregir ahora':'Ver problema'} →</button></div>`).join(''):'<div class="cca-empty">✓ No se detectan asuntos pendientes con los datos registrados.</div>'}</div></section>`;document.getElementById('ccaSearch').oninput=e=>{ST.search=e.target.value;clearTimeout(e.target._t);e.target._t=setTimeout(render,140)};document.getElementById('ccaLevel').onchange=e=>{ST.level=e.target.value;render()};document.getElementById('ccaKind').onchange=e=>{ST.kind=e.target.value;render()};document.getElementById('ccaProject').onchange=e=>{ST.project=e.target.value;render()};document.querySelectorAll('[data-cca-open]').forEach(b=>b.onclick=()=>openItem(list[num(b.dataset.ccaOpen)]))}
function openCenter(){closeOthers();setActive(true);render()}
function closeCenter(){setActive(false)}
window.__ccAlertsCenter={open:openCenter,close:closeCenter,render,state:ST};
window.addEventListener('cc:data-changed',()=>{if(ST.active)setTimeout(render,40)});
ensureCss();
})();
