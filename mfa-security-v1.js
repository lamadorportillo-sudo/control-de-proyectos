/* ===== CONTROL CONTRACTUAL · VERIFICACION EN DOS PASOS V2 ===== */
(()=>{
'use strict';
if(window.__CC_MFA_SECURITY_V1__)return;window.__CC_MFA_SECURITY_V1__=true;
const E=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const endpoint=()=>`${SUPABASE_URL}/functions/v1/secure-mfa`,headers=()=>({apikey:SUPABASE_KEY,Authorization:`Bearer ${session?.accessToken||''}`,'Content-Type':'application/json'});
function saveTokens(d){if(!d?.access_token||!session)return;session={...session,accessToken:d.access_token,refreshToken:d.refresh_token||session.refreshToken,expiresAt:Date.now()+(Number(d.expires_in)||3600)*1000,securitySessionId:d.security_session_id||session.securitySessionId||'',deviceLabel:d.device_label||session.deviceLabel||''};try{localStorage.setItem(SESSION,JSON.stringify(session))}catch{}}
async function call(action,payload={}){if(!session?.accessToken||!session?.refreshToken)throw new Error('La sesión debe renovarse. Ingrese nuevamente.');const r=await fetch(endpoint(),{method:'POST',headers:headers(),body:JSON.stringify({action,refresh_token:session.refreshToken,...payload}),cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'No se pudo completar la verificación en dos pasos.');saveTokens(d);return d}
function safeQr(src){src=String(src||'');return /^data:image\/svg\+xml(?:;charset=[^;,]+)?(?:;base64)?,/i.test(src)?src:''}
const when=v=>{try{return v?new Date(v).toLocaleString('es-HN',{dateStyle:'medium',timeStyle:'short'}):'—'}catch{return v||'—'}};
function css(){if(document.getElementById('cc-mfa-style'))return;const s=document.createElement('style');s.id='cc-mfa-style';s.textContent=`.cc-mfa-wrap{display:grid;gap:12px}.cc-mfa-card{border:1px solid var(--line);border-radius:14px;background:#0a1119;padding:14px}.cc-mfa-qr{display:grid;place-items:center;background:#fff;border-radius:14px;padding:14px;width:min(280px,100%);margin:12px auto}.cc-mfa-qr img{display:block;width:100%;height:auto}.cc-mfa-secret{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere;background:#080d14;border:1px solid var(--line);border-radius:10px;padding:10px}.cc-mfa-code{font-size:22px!important;letter-spacing:.16em;text-align:center}.cc-mfa-factor{display:flex;gap:10px;justify-content:space-between;align-items:center;border:1px solid var(--line);border-radius:11px;padding:10px;background:#0b121c}.cc-mfa-factor small{display:block;color:var(--muted)}#ccMfaAdminNotice{display:flex;gap:12px;align-items:center;justify-content:space-between;margin:0 0 14px;border:1px solid #665b1d;background:#27230c;color:#fef3c7;border-radius:13px;padding:11px 13px}#ccMfaAdminNotice.danger{border-color:#7f1d1d;background:#2b1010;color:#fecaca}#ccMfaAdminNotice small{display:block;opacity:.86}@media(max-width:620px){#ccMfaAdminNotice{align-items:flex-start;flex-direction:column}#ccMfaAdminNotice .btn{width:100%}.cc-mfa-factor{align-items:flex-start;flex-direction:column}.cc-mfa-factor .actions{width:100%}.cc-mfa-factor .actions .btn{flex:1}}`;document.head.appendChild(s)}

async function refreshAdminNotice(){
 css();const old=document.getElementById('ccMfaAdminNotice'),btn=document.getElementById('ccMfaBtn');if(!session?.accessToken||String(cloudRole||'')!=='admin'){old?.remove();if(btn)btn.textContent='2FA';return}
 try{const d=await call('status');if(btn){btn.textContent=d.enabled?'2FA ✓':'2FA · pendiente';btn.classList.toggle('good',!!d.enabled)}if(!d.required||d.enabled){old?.remove();return}const notice=old||document.createElement('div');notice.id='ccMfaAdminNotice';notice.className=d.past_due?'danger':'';notice.innerHTML=`<div><b>${d.past_due?'2FA obligatoria: acceso administrativo restringido':'2FA será obligatoria para administradores'}</b><small>${d.past_due?'Debe activarla para conservar las funciones administrativas.':`Actívela antes de ${E(when(d.required_after))}. Hasta entonces la cuenta continúa funcionando durante el periodo de preparación.`}</small></div><button class="btn primary" type="button">Activar 2FA ahora</button>`;notice.querySelector('button').onclick=mfaModal;if(!old){const top=document.querySelector('.topbar');top?.insertAdjacentElement('afterend',notice)}}catch{}
}

async function mfaModal(){
 css();const m=openModal('Verificación en dos pasos (2FA)',`<div class="alert info"><b>Protección adicional de la cuenta.</b> Puede usar una aplicación autenticadora compatible con códigos TOTP. Una vez activada, la contraseña por sí sola ya no dará acceso al contenido.</div><div id="ccMfaHost"><div class="empty">Consultando protección 2FA…</div></div>`),host=m.querySelector('#ccMfaHost');let pendingFactor='';
 async function load(){pendingFactor='';host.innerHTML='<div class="empty">Actualizando estado 2FA…</div>';try{const d=await call('status'),factors=Array.isArray(d.factors)?d.factors:[],requiredNote=d.required?`<div class="alert ${d.enabled?'good':d.past_due?'danger':'warn'}"><b>Regla administrativa:</b> ${d.enabled?'2FA obligatoria satisfecha.':d.past_due?'El plazo ya venció; debe activar 2FA para continuar como administrador.':`Debe activarla antes de ${E(when(d.required_after))}.`}</div>`:'';
   if(d.enabled){host.innerHTML=`<div class="cc-mfa-wrap">${requiredNote}<div class="alert good"><b>2FA está activado.</b><br>${d.aal==='aal2'?'Esta sesión ya fue verificada con el segundo factor.':'Esta sesión todavía no tiene nivel AAL2; cierre sesión e ingrese nuevamente para completar la verificación.'}</div><div class="cc-mfa-card"><h3>Factores registrados</h3><div class="cc-mfa-wrap">${factors.map(f=>`<div class="cc-mfa-factor"><div><b>${E(f.friendly_name||'Aplicación autenticadora')}</b><small>${E((f.factor_type||'totp').toUpperCase())} · VERIFICADO</small></div>${d.required&&factors.length<=1?'':`<div class="actions"><button type="button" class="btn danger" data-mfa-remove="${E(f.id)}">Desactivar</button></div>`}</div>`).join('')}</div></div><div class="cc-mfa-card"><b>Recuperación</b><p class="notice" style="margin:6px 0 0">Si un administrador pierde el dispositivo autenticador, otro administrador puede restablecer su 2FA desde Usuarios y tendrá 24 horas para configurarlo nuevamente.</p></div></div>`;host.querySelectorAll('[data-mfa-remove]').forEach(b=>b.onclick=async()=>{if(!confirm('¿Desactivar este segundo factor?'))return;b.disabled=true;try{await call('unenroll',{factor_id:b.dataset.mfaRemove});toast('Factor 2FA desactivado.');await load();await refreshAdminNotice()}catch(e){toast(e.message||'No se pudo desactivar 2FA.')}finally{b.disabled=false}});return}
   host.innerHTML=`<div class="cc-mfa-wrap">${requiredNote}<div class="cc-mfa-card"><h3>2FA todavía no está activado</h3><p class="notice">Al activarlo, cada nuevo ingreso necesitará su contraseña y un código temporal generado en su teléfono o aplicación autenticadora.</p><div class="actions"><button type="button" class="btn primary" data-mfa-start>Activar 2FA</button></div></div><div class="cc-mfa-card"><b>Antes de activarlo</b><p class="notice" style="margin:6px 0 0">No comparta el código QR ni la clave secreta. Ambos permiten registrar el segundo factor en otro dispositivo.</p></div></div>`;host.querySelector('[data-mfa-start]').onclick=startEnrollment
  }catch(e){host.innerHTML=`<div class="alert danger"><b>No se pudo consultar 2FA.</b><br>${E(e.message)}</div>`}}
 async function startEnrollment(){host.innerHTML='<div class="empty">Preparando código QR seguro…</div>';try{const d=await call('enroll');pendingFactor=d.factor_id||'';const qr=safeQr(d.qr_code);host.innerHTML=`<div class="cc-mfa-wrap"><div class="cc-mfa-card"><h3>1. Escanee el código QR</h3>${qr?`<div class="cc-mfa-qr"><img alt="Código QR para activar 2FA" src="${E(qr)}"></div>`:'<div class="alert warn">No se pudo mostrar el QR. Use la clave manual.</div>'}<small class="muted">Si su aplicación pide una clave manual:</small><div class="cc-mfa-secret">${E(d.secret||'')}</div><div class="actions"><button type="button" class="btn" data-mfa-copy>Copiar clave manual</button></div></div><div class="cc-mfa-card"><h3>2. Confirme el código</h3><label class="field"><span>Código generado por su aplicación</span><input id="ccMfaEnrollCode" class="cc-mfa-code" inputmode="numeric" autocomplete="one-time-code" maxlength="10" placeholder="000000"></label><div id="ccMfaEnrollMsg" class="notice">Después de verificarlo, esta misma sesión quedará elevada a AAL2.</div><div class="actions"><button type="button" class="btn" data-mfa-cancel>Cancelar</button><button type="button" class="btn primary" data-mfa-confirm>Confirmar y activar</button></div></div></div>`;host.querySelector('[data-mfa-copy]').onclick=()=>navigator.clipboard?.writeText(d.secret||'').then(()=>toast('Clave 2FA copiada.')).catch(()=>toast('No se pudo copiar.'));host.querySelector('[data-mfa-cancel]').onclick=async()=>{try{if(pendingFactor)await call('cancel_enrollment',{factor_id:pendingFactor})}catch{}await load()};host.querySelector('[data-mfa-confirm]').onclick=async e=>{const b=e.currentTarget,code=String(host.querySelector('#ccMfaEnrollCode').value||'').replace(/\D/g,''),msg=host.querySelector('#ccMfaEnrollMsg');if(code.length<6){msg.textContent='Escriba el código generado por la aplicación.';return}b.disabled=true;try{await call('verify_enrollment',{factor_id:pendingFactor,code});pendingFactor='';session.mfaSetupRecommended=false;try{localStorage.setItem(SESSION,JSON.stringify(session))}catch{}toast('Verificación en dos pasos activada.');await load();await refreshAdminNotice()}catch(err){msg.textContent=err.message||'No se pudo verificar el código.';b.disabled=false}};setTimeout(()=>host.querySelector('#ccMfaEnrollCode')?.focus(),50)}catch(e){host.innerHTML=`<div class="alert danger"><b>No se pudo iniciar 2FA.</b><br>${E(e.message)}</div><div class="actions"><button type="button" class="btn" data-mfa-back>Volver</button></div>`;host.querySelector('[data-mfa-back]').onclick=load}}
 await load();
}
function bind(){if(!session?.accessToken){document.getElementById('ccMfaBtn')?.remove();document.getElementById('ccMfaAdminNotice')?.remove();return}if(!document.getElementById('ccMfaBtn')){const anchor=document.getElementById('ccSecurityBtn')||document.getElementById('ccTeamBtn'),host=anchor?.parentElement||document.querySelector('.top-actions');if(host){const b=document.createElement('button');b.id='ccMfaBtn';b.type='button';b.className='btn';b.textContent='2FA';b.title='Configurar verificación en dos pasos';b.onclick=mfaModal;anchor?host.insertBefore(b,anchor):host.appendChild(b)}}setTimeout(refreshAdminNotice,50)}
try{if(typeof renderApp==='function'&&!renderApp.__mfaSecurity){const base=renderApp;renderApp=function(){const r=base.apply(this,arguments);setTimeout(bind,0);return r};renderApp.__mfaSecurity=true}}catch(e){console.warn(e)}
new MutationObserver(bind).observe(document.documentElement,{subtree:true,childList:true});setTimeout(bind,0);window.mfaSecurityModal=mfaModal;window.refreshAdminMfaNotice=refreshAdminNotice;
})();
