/* ===== FOTOGRAFIAS DE VISITAS V1 ===== */
(()=>{
'use strict';
if(window.__CC_VISIT_PHOTOS_V1__)return;window.__CC_VISIT_PHOTOS_V1__=true;
const A=v=>Array.isArray(v)?v:[];
const H=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const say=m=>{try{toast(m)}catch{console.log(m)}};
const MAX_PHOTOS=8,MAX_DIM=1280,TARGET_BYTES=260000;
function css(){if(document.getElementById('cc-visit-photo-style'))return;const s=document.createElement('style');s.id='cc-visit-photo-style';s.textContent=`
.cc-photo-field{grid-column:1/-1;border:1px solid #263b55;background:#09131f;border-radius:12px;padding:11px}.cc-photo-field>span{display:block;font-size:12px;font-weight:800;color:#c8d8e8;margin-bottom:6px}.cc-photo-help{display:block;color:#8196ab;font-size:10px;margin:5px 0 9px}.cc-photo-gallery{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:9px}.cc-photo-card{position:relative;border:1px solid #25374c;background:#081019;border-radius:11px;overflow:hidden;min-height:110px}.cc-photo-card img{display:block;width:100%;aspect-ratio:4/3;object-fit:cover;background:#05090e}.cc-photo-card .cc-photo-meta{padding:7px 8px;font-size:9px;color:#9aadc1;line-height:1.35}.cc-photo-card .cc-photo-meta b{display:block;color:#e6eef7;font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cc-photo-remove{position:absolute;right:6px;top:6px;width:28px;height:28px;border:1px solid rgba(255,255,255,.2);border-radius:50%;background:rgba(5,9,14,.78);color:#fff;font-weight:900;z-index:2}.cc-photo-empty{grid-column:1/-1;border:1px dashed #2b4057;border-radius:10px;padding:13px;text-align:center;color:#7f93a8;font-size:10px}.cc-photo-btn{white-space:nowrap}.cc-photo-view{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:12px}.cc-photo-view .cc-photo-card img{cursor:zoom-in}.cc-photo-pending{display:grid;place-items:center;aspect-ratio:4/3;padding:12px;text-align:center;color:#8da1b5;background:linear-gradient(145deg,#0b1622,#080e16);font-size:10px}.cc-photo-full{position:fixed;inset:0;z-index:120;background:rgba(0,0,0,.92);display:grid;place-items:center;padding:18px}.cc-photo-full img{max-width:96vw;max-height:92vh;object-fit:contain}.cc-photo-full button{position:absolute;right:15px;top:15px;width:42px;height:42px;border:1px solid #526175;border-radius:50%;background:#111b28;color:#fff;font-size:22px}.cc-photo-saving{opacity:.7;pointer-events:none}
@media(max-width:520px){.cc-photo-gallery{grid-template-columns:repeat(2,minmax(0,1fr))}.cc-photo-view{grid-template-columns:1fr 1fr}}
`;document.head.appendChild(s)}
function bytesFromDataUrl(src){const i=String(src||'').indexOf(',');return i<0?0:Math.floor((src.length-i-1)*.75)}
function fmtBytes(n){if(!n)return'';return n<1024?`${n} B`:n<1048576?`${(n/1024).toFixed(0)} KB`:`${(n/1048576).toFixed(1)} MB`}
function loadImage(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onerror=reject;r.onload=()=>{const im=new Image();im.onerror=reject;im.onload=()=>resolve(im);im.src=r.result};r.readAsDataURL(file)})}
function renderCanvas(im,w,h,q){const c=document.createElement('canvas');c.width=w;c.height=h;const x=c.getContext('2d',{alpha:false});x.imageSmoothingEnabled=true;x.imageSmoothingQuality='high';x.drawImage(im,0,0,w,h);return c.toDataURL('image/jpeg',q)}
async function optimize(file){const im=await loadImage(file);let scale=Math.min(1,MAX_DIM/Math.max(im.naturalWidth||im.width,im.naturalHeight||im.height)),w=Math.max(1,Math.round((im.naturalWidth||im.width)*scale)),h=Math.max(1,Math.round((im.naturalHeight||im.height)*scale)),q=.88,src=renderCanvas(im,w,h,q),size=bytesFromDataUrl(src);while(size>TARGET_BYTES&&q>.70){q-=.04;src=renderCanvas(im,w,h,q);size=bytesFromDataUrl(src)}if(size>TARGET_BYTES&&Math.max(w,h)>1050){scale=1050/Math.max(w,h);w=Math.round(w*scale);h=Math.round(h*scale);q=.80;src=renderCanvas(im,w,h,q);size=bytesFromDataUrl(src)}return{id:(typeof uid==='function'?uid():crypto.randomUUID()),name:String(file.name||'foto').replace(/\.[^.]+$/,'')+'.jpg',src,width:w,height:h,size,originalSize:file.size||0,capturedAt:'',createdAt:new Date().toISOString(),optimized:true}}
function photoCard(p,editable=false){const src=p?.src||p?.url||'';return `<article class="cc-photo-card" data-photo-id="${H(p?.id||'')}">${editable?`<button type="button" class="cc-photo-remove" data-photo-remove="${H(p?.id||'')}" title="Quitar fotografía">×</button>`:''}${src?`<img src="${H(src)}" alt="Evidencia fotográfica de la visita" loading="lazy" data-photo-zoom>`:`<div class="cc-photo-pending">Fotografía registrada<br>pendiente de archivo digital</div>`}<div class="cc-photo-meta"><b>${H(p?.name||'Fotografía')}</b>${p?.capturedAt?`${H(new Date(p.capturedAt).toLocaleString('es-HN'))}<br>`:''}${p?.size?`Optimizada · ${H(fmtBytes(p.size))}`:(p?.status?H(p.status):'')}</div></article>`}
function preview(form){const box=form.querySelector('[data-photo-preview]');if(!box)return;const photos=A(form.__ccVisitPhotos);box.innerHTML=photos.length?photos.map(p=>photoCard(p,true)).join(''):'<div class="cc-photo-empty">Todavía no hay fotografías adjuntas.</div>';box.querySelectorAll('[data-photo-remove]').forEach(b=>b.onclick=()=>{form.__ccVisitPhotos=photos.filter(x=>String(x.id)!==String(b.dataset.photoRemove));preview(form)})}
function currentVisitForForm(form){try{const pid=view?.projectId,number=Number(form.querySelector('#vNumber')?.value||0);return A(db?.visits).find(v=>v.projectId===pid&&Number(v.number)===number)||null}catch{return null}}
function enhanceForm(form){if(!form||form.dataset.ccPhotos==='1')return;form.dataset.ccPhotos='1';const existing=currentVisitForForm(form);form.__ccVisitPhotos=A(existing?.photos).map(x=>({...x}));const actions=form.querySelector('.modal-actions');if(!actions)return;const field=document.createElement('div');field.className='cc-photo-field';field.innerHTML=`<span>Fotografías de la visita</span><input id="vPhotos" type="file" accept="image/*" multiple><small class="cc-photo-help">Puedes adjuntar hasta ${MAX_PHOTOS} fotografías. El sistema reduce automáticamente el peso, conserva la proporción y evita recortes para mantener el detalle de supervisión.</small><div class="cc-photo-gallery" data-photo-preview></div>`;actions.before(field);preview(form);const input=field.querySelector('#vPhotos');input.onchange=async()=>{const files=[...input.files||[]];if(!files.length)return;const room=Math.max(0,MAX_PHOTOS-form.__ccVisitPhotos.length);if(!room){say(`Máximo ${MAX_PHOTOS} fotografías por visita.`);input.value='';return}field.classList.add('cc-photo-saving');say('Optimizando fotografías…');try{for(const f of files.slice(0,room)){if(!/^image\//i.test(f.type||''))continue;form.__ccVisitPhotos.push(await optimize(f))}preview(form);say('Fotografías listas y optimizadas.')}catch(e){console.error(e);say('No se pudo procesar una de las fotografías.')}finally{field.classList.remove('cc-photo-saving');input.value=''}};form.addEventListener('submit',()=>{const pid=view?.projectId,number=Number(form.querySelector('#vNumber')?.value||0),photos=A(form.__ccVisitPhotos).map(x=>({...x}));setTimeout(()=>{try{const v=A(db?.visits).find(x=>x.projectId===pid&&Number(x.number)===number);if(!v)return;v.photos=photos;v.photoCount=photos.length;v.photoCompression={maxDimension:MAX_DIM,targetBytes:TARGET_BYTES,format:'JPEG',mode:'proporcional-sin-recorte',updatedAt:new Date().toISOString()};v.updatedAt=typeof iso==='function'?iso():new Date().toISOString();if(typeof audit==='function')audit('ACTUALIZAR','Fotografías de visita',v.id,{projectId:pid,visitNumber:number,photoCount:photos.length});if(typeof saveDB==='function')saveDB();if(typeof renderProject==='function')renderProject();say(`Visita guardada con ${photos.length} fotografía${photos.length===1?'':'s'}.`)}catch(e){console.error(e)}},80)})}
function openViewer(v){const photos=A(v?.photos);const content=photos.length?`<div class="cc-photo-view">${photos.map(p=>photoCard(p,false)).join('')}</div>`:'<div class="empty">Esta visita no tiene fotografías adjuntas.</div>';let m;try{m=openModal(`Fotografías · Visita N.º ${v.number}`,content)}catch{return}m.querySelectorAll('[data-photo-zoom]').forEach(im=>im.onclick=()=>{const full=document.createElement('div');full.className='cc-photo-full';full.innerHTML=`<button type="button" aria-label="Cerrar">×</button><img src="${H(im.src)}" alt="Fotografía ampliada">`;document.body.appendChild(full);full.onclick=e=>{if(e.target===full||e.target.tagName==='BUTTON')full.remove()}})}
function decorateVisitTable(){let pid;try{pid=view?.projectId}catch{return}if(!pid)return;document.querySelectorAll('#tabBody .table tbody tr').forEach(tr=>{if(tr.dataset.ccPhotos==='1')return;const first=tr.querySelector('td');if(!first)return;const number=Number(first.textContent.trim());if(!number)return;const v=A(db?.visits).find(x=>x.projectId===pid&&Number(x.number)===number);if(!v)return;const cells=tr.querySelectorAll('td'),cell=cells[cells.length-1];if(!cell)return;const b=document.createElement('button');b.type='button';b.className='btn cc-photo-btn';b.textContent=`Fotos (${A(v.photos).length})`;b.onclick=()=>openViewer(v);cell.append(' ',b);tr.dataset.ccPhotos='1'})}
function run(){css();const f=document.getElementById('visitForm');if(f)enhanceForm(f);decorateVisitTable()}
const mo=new MutationObserver(()=>queueMicrotask(run));mo.observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest?.('[data-edit-visit],#newVisit,[data-visit-obs]'))setTimeout(run,0)},true);run();
})();