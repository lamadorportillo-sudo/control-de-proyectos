/* ===== CONTROL CONTRACTUAL · CARGADOR DEL NÚCLEO DE INTEGRIDAD ===== */
(()=>{
  if(window.__CC_CORE_HARDENING_V1__||document.getElementById('ccCoreHardeningLoader'))return;
  const s=document.createElement('script');
  s.id='ccCoreHardeningLoader';
  s.src='core-hardening-v1.js?v=20260820-hardening1';
  s.async=false;
  document.head.appendChild(s);
})();

/* ===== CONTROL CONTRACTUAL · EQUIPO, INVITACIONES Y OFFLINE V1 ===== */
(()=>{
'use strict';
if(window.__CC_WORKSPACE_ACCESS_V1__)return;window.__CC_WORKSPACE_ACCESS_V1__=true;
const H=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const say=m=>{try{toast(m)}catch{console.log(m)}};
function authInvite(){const f=document.getElementById('authForm');if(!f||document.getElementById('authInvite'))return;const name=document.getElementById('authName'),host=name?.closest('.field')||f.querySelector('.reg');if(!host)return;const lab=document.createElement('label');lab.className='field reg hidden';lab.innerHTML='<span>Código de invitación <small style="font-weight:500">(opcional)</small></span><input id="authInvite" autocomplete="off" placeholder="Ej. A1B2C3D4E5F6"><small>Úsalo si un administrador te invitó al mismo Control Contractual.</small>';host.insertAdjacentElement('afterend',lab)}
async function invitedSignup(ev){const f=ev.target;if(f?.id!=='authForm')return;const reg=document.getElementById('registerTab'),invite=document.getElementById('authInvite'),isReg=reg?.classList.contains('active')||/crear/i.test(document.getElementById('authSubmit')?.textContent||'');if(!isReg||!invite?.value.trim())return;ev.preventDefault();ev.stopImmediatePropagation();const email=document.getElementById('authEmail')?.value.trim().toLowerCase(),password=document.getElementById('authPass')?.value||'',name=document.getElementById('authName')?.value.trim(),code=invite.value.trim().toUpperCase(),btn=document.getElementById('authSubmit'),msg=document.getElementById('authMessage');if(!name||!email||password.length<8)return say('Completa nombre, correo y una contraseña de al menos 8 caracteres.');btn.disabled=true;if(msg)msg.textContent='Validando invitación…';try{const r=await fetch(SUPABASE_URL+'/auth/v1/signup',{method:'POST',headers:{apikey:SUPABASE_KEY,'Content-Type':'application/json'},body:JSON.stringify({email,password,data:{full_name:name,workspace_invite_code:code}})}),d=await r.json();if(!r.ok)throw new Error(d.msg||d.message||d.error_description||'No se pudo usar la invitación.');if(!d.access_token){if(msg)msg.textContent='Cuenta creada. Confirma tu correo y luego ingresa; quedarás vinculado al mismo espacio de trabajo.';return}session={userId:d.user.id,email:d.user.email,accessToken:d.access_token,refreshToken:d.refresh_token,expiresAt:Date.now()+(d.expires_in||3600)*1000};localStorage.setItem(SESSION,JSON.stringify(session));cloudLoaded=false;await render()}catch(e){if(msg)msg.textContent=e.message||'No se pudo crear el acceso.'}finally{btn.disabled=false}}
function teamButton(){let u;try{u=currentUser()}catch{return}if(!u||u.role!=='admin')return;const box=document.querySelector('.userbox');if(!box||document.getElementById('ccTeamBtn'))return;const b=document.createElement('button');b.type='button';b.className='btn';b.id='ccTeamBtn';b.textContent='Equipo';const out=document.getElementById('logoutBtn');box.insertBefore(b,out||null);b.onclick=teamModal}
function teamModal(){if(typeof openModal!=='function')return;openModal('Equipo y accesos',`<div class="stack"><div class="alert info">Crea un código temporal para que otro usuario entre al mismo espacio de trabajo. No compartas tu contraseña.</div><label class="field"><span>Rol</span><select id="ccInviteRole"><option value="consulta">Consulta · solo lectura</option><option value="editor">Editor · puede registrar y actualizar</option></select></label><label class="field"><span>Vigencia del código</span><select id="ccInviteDays"><option value="1">1 día</option><option value="3">3 días</option><option value="7" selected>7 días</option><option value="14">14 días</option></select></label><button class="btn primary" id="ccCreateInvite">Generar código de invitación</button><div id="ccInviteResult"></div></div>`);document.getElementById('ccCreateInvite').onclick=async()=>{const btn=document.getElementById('ccCreateInvite'),out=document.getElementById('ccInviteResult');btn.disabled=true;try{const r=await sbFetch('/rest/v1/rpc/create_workspace_invite',{method:'POST',body:{p_role:document.getElementById('ccInviteRole').value,p_days:Number(document.getElementById('ccInviteDays').value)}}),x=Array.isArray(r.data)?r.data[0]:r.data;if(!x?.invite_code)throw new Error('No se recibió el código.');out.innerHTML=`<div class="panel" style="margin-top:10px"><small class="muted">CÓDIGO DE INVITACIÓN</small><div style="font-size:22px;font-weight:900;letter-spacing:.12em;margin:6px 0">${H(x.invite_code)}</div><small class="muted">Rol: ${H(x.role)} · vence ${new Date(x.expires_at).toLocaleString('es-HN')}</small><div class="actions"><button class="btn" id="ccCopyInvite">Copiar código</button></div></div>`;document.getElementById('ccCopyInvite').onclick=()=>navigator.clipboard?.writeText(x.invite_code).then(()=>say('Código copiado.')).catch(()=>say('Código: '+x.invite_code))}catch(e){out.innerHTML=`<div class="alert danger">${H(e.message||'No se pudo generar la invitación.')}</div>`}finally{btn.disabled=false}}}
try{if(typeof renderAuth==='function'&&!renderAuth.__ccInvite){const base=renderAuth;renderAuth=function(){const r=base.apply(this,arguments);setTimeout(authInvite,0);return r};renderAuth.__ccInvite=true}}catch(e){console.warn(e)}
try{if(typeof renderApp==='function'&&!renderApp.__ccTeam){const base=renderApp;renderApp=function(){const r=base.apply(this,arguments);setTimeout(teamButton,0);return r};renderApp.__ccTeam=true}}catch(e){console.warn(e)}
try{if(typeof scheduleCloudSave==='function'&&!scheduleCloudSave.__ccOffline){const base=scheduleCloudSave;const safe=function(){if(!navigator.onLine){localStorage.setItem('cc_pending_sync','1');say('Sin conexión: los cambios quedan como borrador local hasta recuperar internet.');return}return base.apply(this,arguments)};safe.__ccOffline=true;scheduleCloudSave=safe}}catch(e){console.warn(e)}
document.addEventListener('submit',ev=>{invitedSignup(ev);if(!navigator.onLine&&['projectForm','contractForm','estForm','chForm','gForm','budgetMoveForm'].includes(ev.target?.id))setTimeout(()=>say('Borrador local guardado. La validación definitiva se realizará al sincronizar con PostgreSQL.'),0)},true);
window.addEventListener('online',()=>{if(localStorage.getItem('cc_pending_sync')==='1'){say('Conexión recuperada. Validando cambios pendientes…');Promise.resolve(typeof saveCloudNow==='function'?saveCloudNow():null).then(()=>{localStorage.removeItem('cc_pending_sync');say('Cambios pendientes sincronizados.')}).catch(()=>say('Hay cambios pendientes que requieren revisión antes de sincronizar.'))}});
setTimeout(authInvite,0);setTimeout(teamButton,0);
})();
